node_modules/
.DS_Store
.claude/
*.local

# Raw proxy captures (large, contain API traffic)
proxy.bodies.jsonl
proxy.log.json

# Artefacts written by experiments outside test-env
src/constants.py

# Posts (managed separately)
REDDIT_POST.md
